name: Deploy to GitHub Pages

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build demo
        run: |
          cd packages/asciitorium
          npm run build

      - name: Copy demo files for Pages
        run: |
          mkdir -p dist-pages
          cd packages/asciitorium

          # Copy main HTML file
          cp index.html ../../dist-pages/

          # Copy all public assets (CSS, fonts, images, art files)
          cp -r public/* ../../dist-pages/ 2>/dev/null || true

          # Copy built library files
          cp -r dist ../../dist-pages/

          # Copy source files (needed for the demo since it imports from src)
          cp -r src ../../dist-pages/

      - name: Setup Pages
        uses: actions/configure-pages@v4

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: './dist-pages'

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    if: github.ref == 'refs/heads/main'
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4