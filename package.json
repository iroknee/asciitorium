{
  "name": "asciitorium-monorepo",
  "private": true,
  "version": "0.1.0",
  "description": "Monorepo for asciitorium and create-asciitorium",
  "scripts": {
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "release": "pnpm -r publish",
    "test:lib": "scripts/pack-lib-test.sh",
    "test:create": "scripts/create-asciitorium-test.sh",
    "publish:lib": "pnpm --filter asciitorium build && cd packages/asciitorium && pnpm publish",
    "publish:create": "pnpm --filter create-asciitorium build && cd packages/create-asciitorium && pnpm publish"

  },
  "workspaces": [
    "packages/*"
  ]
}
